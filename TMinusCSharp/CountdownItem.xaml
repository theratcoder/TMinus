<Page
    x:Class="TMinusCSharp.CountdownItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TMinusCSharp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    PointerPressed="OnClick"
    Tapped="OnTap" PointerEntered="pointerIn"
    x:Name="page" PointerExited="pointerOut">

    <Grid BorderBrush="Black" BorderThickness="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="titleTxt" Style="{ThemeResource SubtitleTextBlockStyle}" Grid.Column="0" Margin="2,2,2,2"/>
        <TextBlock x:Name="CDTxt" Style="{ThemeResource SubtitleTextBlockStyle}" Grid.Column="1" Margin="2,2,2,2"/>
    </Grid>
</Page>
